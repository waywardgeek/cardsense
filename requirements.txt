# Core dependencies (cross-platform)
opencv-python>=4.8.0
numpy>=1.24.0
mss>=9.0.0
pytesseract>=0.3.10
requests>=2.31.0

# Platform-specific TTS
pyobjc-framework-Cocoa>=10.0; sys_platform == 'darwin'
pyttsx3>=2.90; sys_platform == 'win32'

# Build tools (optional, for creating installers)
pyinstaller>=6.0.0
